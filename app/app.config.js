(function(){
    'use strict';
    angular.module("app").config(config).run()
    function config($locationProvider){
        // $locationProvider.hashPrefix("")
        // $locationProvider.html5Mode(true)
    }
})()